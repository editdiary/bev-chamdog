# loss 재설계의 영향력 — 확정 config에서의 대조 실험 결과

**2026-09-02. 산출물 `runs/loss_effect/`. 25런(사다리 5칸 × 시드 5개).**
정본 환경 `bev-chamdog`(Python 3.11.15 · torch 2.7.0+cu128 · numpy 1.26.4).
결과 데이터 한 파일: **[`runs/loss_effect/analysis/RESULTS.json`](../runs/loss_effect/analysis/RESULTS.json)**
(폴더 안내는 같은 곳 `README.md`).

실험 계획 정본 [`docs/temp/loss_stability_evaluation_plan_for_claude_2026-09-01.md`](temp/loss_stability_evaluation_plan_for_claude_2026-09-01.md),
검토 코멘트 [`docs/temp/loss_effect_review_for_claude_2026-09-02.md`](temp/loss_effect_review_for_claude_2026-09-02.md).
학습 [`configs/loss_effect.sh`](../configs/loss_effect.sh), 분석 [`configs/loss_effect_analysis.sh`](../configs/loss_effect_analysis.sh).

> **이 문서는 이전 loss 실험과 독립이다.** 확정 config가 세 번 바뀌었으므로(§1.2) 2026-08-28
> 이전의 런은 **지금 코드와 다른 모델을 학습한다.** 옛 숫자와 한 표에 세우지 않는다.

---

## 0. 한 문단 요약

**되올림을 만든 것은 경계 대역을 hard 0/1로 감독한 것이고, soft target이 그것을 없앤다.**
`C_hard`(집계 방식은 새 loss와 같고 **대역 target만 hard**)의 val loss 되올림은
**47.6 %** 로 기존 CE(45.0 %)와 사실상 같다. 대역 target만 soft로 바꾸면 **0.5 %** 가 된다.
그리고 그 되올림은 **경계 대역에 국소화돼 있다** — CE에서 val 오차 증가분의 **96.5 %**가
셀의 12.7 %인 대역에서 나오고, 대역 밖은 40 epoch 동안 +4.2 %만 움직인다.

부수 효과로 **목적함수가 모델 선택 신호로 회복된다**(val loss 최저점과 품질 정점의 거리
22.6 → 2.2 epoch, 그때 잃는 `iou_free` 0.0161 → 0.0004) 그리고 **동작점 재현성이 크게
개선된다**(같은 안전 수준을 만드는 문턱의 시드 간 산포 0.087 → 0.009, 9.7배).

**정확도는 바뀌지 않았고**(`iou_free` 0.8115 → 0.8130), **예측 기하와 셀 단위 결정의
재현성도 바뀌지 않았다**(광선 산포 4.11 → 4.47 cm, 결정 불일치 1.45 → 1.44 %).
τ=0.5에서 보이는 `fatal` 개선은 **동작점 이동**이다 — 같은 `free_miss`에서 곡선이 겹친다.

---

## 1. 실험 설계

### 1.1 사다리 — 한 계단에 손잡이 하나

| 칸 | loss | 대역 target | 무엇이 추가되나 |
|---|---|---|---|
| `A_ce` | `weighted_ce` | hard (전 셀) | **대조군.** 역빈도 가중 CE |
| `B_perset` | `soft_boundary`, λ_B=0 | **없음** | per-set 평균 + 대역 감독 제거 |
| `C_hard` | + λ_B=0.5, σ→0 | **hard 0/1** | 대역에 hard 감독 |
| `C_soft` | 같은 것, σ=0.10 | **soft** | **대역 target만 hard → soft** |
| `D_range` | + λ_R=0.3 | soft | 자유거리 보조항 `L_range` |

**주 비교는 `A_ce` ↔ `D_range`**(기존 BCE 대 튜닝한 loss)이고, 원인 귀속은 아래 두 쌍이
담당한다.

| 비교 | 무엇이 갈리나 |
|---|---|
| **`A_ce` vs `C_hard`** | **집계 방식만.** 둘 다 hard target으로 모든 셀을 감독한다 |
| **`C_hard` vs `C_soft`** | **대역 target만.** hard 0/1 ↔ soft (나머지 전부 동일) |
| `C_soft` vs `D_range` | `L_range` |

> **`C_hard`는 코드 변경 없이 만든다.** gaussian target은 `σ → 0`에서 계단 함수로
> 수렴하고, 5 cm 격자에서 대역 안 셀 중심의 `|d|`가 최소 0.05 m이므로 `SIGMA_M=0.001`이면
> target이 **정확히 0/1**이 된다. 실측 target 엔트로피 `H̄ = 0.000000`
> (`C_soft`는 0.264622).

### 1.2 고정한 것

`Y=4`(높이 bin, −0.25~1.75 m) · `δ=0.30` · `λ_B=0.5` · `λ_R=0.3` · `δ_R=0.15` · `β=0.15` ·
`κ=1.0` · `ε=0.0` · res101 · binary · lr 1e-4 · batch 8 · 40 epoch · augment on ·
train `raws2,raws3,rawos1,rawos2,rawos4`(192프레임) · val `raws1,rawos3`(75프레임).

**loss 이외 전부 동일**하고 시드 0~4가 모든 칸에서 같은 ID다.

> 이 실험이 이전 실험과 독립인 이유: `Y=1→4`(2026-08-27, 기하가 다르다),
> soft target `(δ,α)=(0.15,0.5) → (δ,σ)=(0.30,0.10)`(2026-08-28),
> `(δ_R,β)=(0.20,0.10) → (0.15,0.15)`(2026-09-01). 세 번 다 확정 config 변경이다.

### 1.3 판정 기준 — 두 가지 노이즈를 실측했다

| | 값 | 어떻게 쟀나 |
|---|---|---|
| **σ_run** (재실행 노이즈) | `iou_free` **0.0009** | 같은 config·같은 시드를 따로 실행한 **18쌍**(`runs/archive/loss_effect_v1` 대조). cuDNN 비결정성이 만드는 바닥 |
| **σ_seed** (시드 간, ep40) | `iou_free` 0.0016 (`A_ce`) | 이 실험 |

**시드 분산이 재실행 노이즈의 1.8배**다. 즉 n=5의 평균 차이가 0.002보다 작으면 이 실험으로
판정할 수 없다. 분산비 검정은 `F(4,4)` 상위 5 % = 6.39, 즉 **σ비 2.53배**를 넘어야 유의다.

### 1.4 ⚠ 재현성을 어느 epoch에서 재나 — 두 기준을 모두 본다

체크포인트 선택 규칙은 **val `iou_free` 최고점**이다. 그런데 그것은 40 epoch에 대한
**argmax이므로 그 자체가 확률변수**다. 선택 epoch에서 잰 "시드 간 산포"에는 두 가지가
섞인다: ① 시드가 만든 차이, ② 선택이 곡선의 어디에 떨어졌나. 실측하면 후자가 더 크다.

| 칸 | 선택 ep | **런 안에서 곡선이 흔들리는 폭** (ep20~40 `fatal`의 런 내부 sd) | 시드 간 sd @선택 ep | 시드 간 sd @ep40 |
|---|---|---|---|---|
| `A_ce` | 30.8±6.9 | **0.0045** | 0.0040 | 0.0012 |
| `C_soft` | 32.8±7.7 | **0.0020** | 0.0007 | 0.0012 |

`A_ce`는 **한 런 안에서** epoch만 바꿔도 `fatal`이 0.0045 흔들리는데, 고정 epoch의 시드 간
산포는 0.0012다. **"어느 epoch에 떨어졌나"가 "어느 시드였나"보다 큰 변동원**이고, 곡선의
평평함이 칸마다 달라서(0.0045 대 0.0020) 그 오염의 크기도 칸마다 다르다.

**그래서 이 문서는 둘 다 보고하고 역할을 나눈다.**

- **평균**은 선택 epoch이 맞다 — 실제로 배포되는 모델이다.
- **재현성 주장**은 **두 기준에서 모두 성립하는 것만** 한다.
- 선택 epoch 산포가 무의미한 것은 아니다. 그것은 *"배포될 모델들이 서로 얼마나 다른가"*라는
  다른 질문의 답이고 실무적으로 중요하다. 다만 **loss의 성질과 선택 규칙의 성질을 합친
  값**이라 "이 loss가 학습을 더 재현 가능하게 만든다"의 근거로는 쓸 수 없다.

### 1.5 다른 한계

- **val이 75프레임 / 시퀀스 2개**다. 계획서 §7(시퀀스 단위 일반화)은 이 split으로 답할 수
  없다(§8).
- 라벨 경계 자체에 어노테이션 불확실성이 있어 `iou_free`/`f1@τ`의 **작은** 차이를 물리적
  정확도의 우열로 읽기 어렵다. 그래도 **"차이가 없었다"는 것은 보고한다** — 그것이 이
  실험의 결과 중 하나이고, 감추면 주장이 반증 불가능해진다.

---

## 2. 되올림의 원인 — **hard target이다** (계획서 §2, 검토서 P2)

### 2.1 되올림

하한 `λ_B·H̄`를 뺀 excess 기준. 평균 ± 시드 간 SD (n=5).

| 칸 | 대역 target | 되올림 [%] |
|---|---|---|
| `A_ce` | hard | **45.0 ± 13.1** |
| `B_perset` | 없음 | 0.5 ± 0.4 |
| **`C_hard`** | **hard** | **47.6 ± 8.6** |
| **`C_soft`** | **soft** | **0.5 ± 0.5** |
| `D_range` | soft | 0.6 ± 0.4 |

**`C_hard`는 `C_soft`와 집계 방식·가중치·δ·모든 것이 같고 대역 target만 hard다. 그런데
되올림이 CE와 같다.** 즉

- **집계 방식(per-set 평균)은 되올림의 원인이 아니다** — `A_ce`(45.0) → `C_hard`(47.6)에서
  안 줄어든다.
- **대역 target을 soft로 바꾸는 것이 원인이다** — `C_hard`(47.6) → `C_soft`(0.5),
  **95배 감소.** 다섯 시드가 전부 겹치지 않는다(`C_hard` 37.1~59.2, `C_soft` 0.0~1.3).

> **이전 판본의 정정.** 2026-09-01 판은 "되올림을 없앤 것은 per-set 평균이다"라고 적었다.
> 그것은 `A_ce → B_perset` 한 계단에서 **두 손잡이가 동시에 바뀐 것**(집계 방식 + 대역
> 감독 제거)을 한쪽에 귀속시킨 오류였다. `C_hard`가 그 분해를 하고, 결론은 반대다.

### 2.2 되올림은 경계에 국소화돼 있다 (검토서 P3)

각 런의 `val/loss_epoch`은 자기 loss의 값이라 칸끼리 비교할 수 없다. 그래서 **loss와 무관한
공통 눈금**을 학습 중에 따로 기록했다 — hard 0/1 target에 대한 CE를 `|d| ≤ 0.15 m`
(valid 셀의 **12.68 %**, 모든 칸에서 동일)와 그 밖으로 나눈 값이다
(`binary_metrics.region_ce_diagnostics`, gradient에 들어가지 않는 관측값).

| 칸 | 경계 대역 CE (최저→끝) | 대역 밖 CE (최저→끝) | **증가분 중 대역의 몫** |
|---|---|---|---|
| `A_ce` | 0.754 → 1.246 (**+65.3 %**) | 0.0639 → 0.0665 (+4.2 %) | **96.5 %** |
| `B_perset` | — (+ 큼) | (+4.5 %) | 96.7 % |
| `C_hard` | 0.652 → 1.239 (**+90.1 %**) | 0.0675 → 0.0721 (+7.3 %) | **94.9 %** |
| `C_soft` | 0.602 → 0.644 (**+6.9 %**) | 0.0495 → 0.0496 (+0.1 %) | 99.2 % |
| `D_range` | 0.601 → 0.640 (+6.5 %) | 0.0523 → 0.0523 (+0.1 %) | 99.1 % |

**train 쪽은 다섯 칸 모두 마지막 epoch까지 단조 하강한다(+0.0 %).** 즉 val의 경계 성분만
오른다.

*주장할 수 있는 것:* **"CE의 validation 오차 증가는 경계 대역에 국소화돼 있다(증가분의
96.5 %가 셀의 12.7 %에서). soft target은 그 경계 성분의 상승을 65 % → 7 %로 줄인다."**
이것은 문제 정의와 해결책을 직접 연결하는 근거다.
*주장하면 안 되는 것:* 대역 밖이 좋아졌다는 것. 대역 밖도 조금은 오른다(+4.2 % → +0.1 %로
줄지만 절대값이 작아 기여가 미미하다).

> 진단 문서 §26이 옛 config에서 "val loss 증가분의 90 %가 경계 ±20 cm에서 온다"고 쟀던 것과
> 같은 방향이고, 여기서는 **loss에 의존하지 않는 눈금**으로 현재 config에서 재확인했다.

### 2.3 목적함수가 모델 선택 신호로 회복된다

| 칸 | val 최저 ep | Δep(loss↔iou) | regret `iou_free` | regret `f1@10cm` |
|---|---|---|---|---|
| `A_ce` | 8.2 ± 2.3 | 22.6 ± 6.9 | 0.0161 ± 0.0069 | 0.0697 ± 0.0352 |
| `B_perset` | 37.4 ± 2.3 | 32.0 ± 3.5 | 0.0195 ± 0.0065 | 0.0382 ± 0.0108 |
| **`C_hard`** | **5.6 ± 1.1** | **19.2 ± 3.0** | **0.0117 ± 0.0040** | 0.0371 ± 0.0059 |
| **`C_soft`** | **31.4 ± 7.7** | **2.2 ± 3.9** | **0.0004 ± 0.0006** | 0.0043 ± 0.0038 |
| `D_range` | 29.4 ± 9.2 | 9.8 ± 7.5 | 0.0007 ± 0.0007 | 0.0029 ± 0.0033 |

*무엇을 재는가:* "만약 val loss로 체크포인트를 골랐다면 얼마나 손해였나." 우리의 실제 규칙은
`iou_free`이므로 가정 실험이다.

*결과:* CE로 학습하고 val loss로 멈추면 `iou_free`를 **0.0161** 잃는다(시드 σ의 10배).
`C_hard`도 거의 같다(0.0117, 최저점이 epoch 5.6). **soft target을 넣으면 0.0004로 40배
줄고**(Δep 22.6 → 2.2) 최저점이 epoch 31.4로 옮겨 간다.

*주장할 수 있는 것:* **"기존 CE의 validation objective는 일찍 품질 proxy와 분리되어 학습
진행도를 반영하지 못했다. soft boundary supervision 이후에는 목적함수의 최저점이 품질
정점과 다시 정렬되어, 별도의 품질 지표 없이도 목적함수 자체로 학습 진행도를 읽을 수 있다."**
*주장하면 안 되는 것:* "val 라벨 없이 멈출 자리를 안다" — **틀린 표현이다.** validation
loss도 GT 라벨을 쓴다. 달라진 것은 *"IoU/F1 같은 별도 품질 지표를 선택 신호로 쓰지 않아도
된다"*는 것뿐이다. (이전 판본의 표현을 정정한다.)

### 2.4 epoch 간 변동 (보조)

plateau ep 10~40에서 `|L(e) − L(e−1)|` 평균: `A_ce` 0.00503, `C_soft` 0.00169,
`D_range` 0.00238. 방향만 읽는다 — **loss의 절대 크기가 칸마다 달라 칸끼리 직접 비교하는
것은 엄밀하지 않다**(계획서가 이 지표의 우선순위를 낮춘 이유).

---

## 3. 정확도와 최종 지표 재현성 (계획서 §3)

### 3.1 정확도 — 바뀌지 않았다

체크포인트 선택 epoch, 평균 ± 시드 간 SD (n=5).

| 지표 | `A_ce` | `B_perset` | `C_hard` | `C_soft` | `D_range` |
|---|---|---|---|---|---|
| `iou_free` ↑ | 0.8115±0.0009 | 0.6944±0.0179 | 0.8115±0.0013 | **0.8130**±0.0008 | 0.8126±0.0005 |
| `f1@10cm` ↑ | 0.6098±0.0061 | 0.2462±0.0406 | **0.6120**±0.0069 | 0.6010±0.0040 | 0.6032±0.0057 |
| `fatal_rate` ↓ | 0.1284±0.0040 | 0.2571±0.0210 | 0.1214±0.0021 | 0.1215±0.0007 | **0.1191**±0.0007 |
| `free_miss_rate` ↓ | **0.0756**±0.0053 | 0.0506±0.0091 | 0.0838±0.0023 | 0.0815±0.0014 | 0.0849±0.0015 |
| `range_mae` [m] ↓ | **0.2097**±0.0038 | 0.3805±0.0201 | 0.2112±0.0042 | 0.2139±0.0032 | 0.2114±0.0029 |
| `missed_obstacle` ↓ | 0.0406±0.0024 | 0.1125±0.0125 | **0.0356**±0.0015 | 0.0399±0.0013 | 0.0372±0.0009 |

*결과:* `A_ce` → `D_range`의 `iou_free` 차이는 +0.0011로 σ_seed(0.0009) 규모이고 판정
불가다. `f1@10cm`은 −0.0066으로 오히려 조금 낮다. `fatal`↓/`free_miss`↑는 §6.2가 동작점
이동으로 설명한다.

**`C_hard`가 중요한 참고점이다** — 되올림은 CE와 같은데 정확도는 `C_soft`와 사실상 같다
(`iou_free` 0.8115, `f1@10cm` 0.6120으로 오히려 최고). **즉 hard 감독은 모델을 망치지
않는다. 목적함수의 거동을 망친다.** 반면 `B_perset`(대역 감독 제거)은 모델 자체가
무너진다(`iou_free` 0.694) — **"경계가 문제니 경계를 무시하자"가 아니라는 것**을 이 칸이
못박는다.

### 3.2 최종 지표 재현성 — 개선되지 않았다

시드 간 표준편차의 비 (`A_ce` / 해당 칸). **유의 문턱 2.53배.**

| 지표 | 선택 epoch `A/C_soft` | **@ep40 `A/C_soft`** | 선택 epoch `A/D` | **@ep40 `A/D`** |
|---|---|---|---|---|
| `iou_free` | 1.1배 | 1.9배 | 1.8배 | **3.1배** |
| `fatal_rate` | 5.7배 | 1.0배 | 5.7배 | 1.3배 |
| `free_miss` | 3.8배 | 2.5배 | 3.5배 | 2.1배 |
| `f1@10cm` | 1.5배 | 1.2배 | 1.1배 | 1.1배 |
| `range_bias` | 7.4배 | 1.6배 | 11.8배 | 1.8배 |

*결과:* **선택 epoch에서는 커 보이는 배율이 고정 epoch에서 대부분 사라진다**
(`fatal` 5.7배 → 1.0배, `range_bias` 7.4배 → 1.6배). §1.4가 설명한 오염이다. 두 기준에서
모두 문턱을 넘는 지표는 **하나도 없다**.

*주장할 수 있는 것:* **"최종 품질 지표의 재학습 재현성이 개선되었다는 근거는 없다."**
*주장하면 안 되는 것:* "soft boundary supervision이 재현성을 3~4배 개선한다" —
**2026-09-01 판본의 이 주장을 철회한다.** 선택 epoch만 보고 낸 결론이었다.

### 3.3 전역 자유거리 편향의 산포 — 여기서는 개선된다

시드별 평균 자유거리의 표준편차 [cm] (`σ_전역`, 계획서 §3.2의 `V_global`).

| `A_ce` | `B_perset` | `C_hard` | `C_soft` | `D_range` |
|---|---|---|---|---|
| 1.27 | 3.61 | 0.89 | **0.32** | **0.28** |

*결과:* `A_ce` → `D_range` **4.5배** 감소. 계단별로는 집계 방식이 1.4배(1.27 → 0.89),
soft target이 2.8배(0.89 → 0.32)를 기여한다. **두 변경이 모두 기여하고 soft target 쪽이
크다.** 단 이것은 선택 체크포인트 기준이므로 §3.2의 유보가 부분적으로 적용된다 —
`range_bias`의 σ가 고정 epoch에서 1.8배로 줄어드는 것과 같이 읽어야 한다.

---

## 4. 예측 기하 안정성 (계획서 §4) — 개선되지 않았다

다섯 시드가 **모두** `RAY_OK`인 광선에서 잰 시드 간 표준편차.

| 칸 | `iou_free` | σ_전역 | σ_ray median | σ_ray P90 | σ_잔차 median | 상태 불일치 | \|오차\| median |
|---|---|---|---|---|---|---|---|
| `A_ce` | 0.8115 | 1.27 | **4.11** cm | 15.37 cm | **4.00** cm | **18.53 %** | 11.50 cm |
| `B_perset` | 0.6944 | 3.61 | 7.83 | 21.55 | 6.71 | 43.36 % | 25.00 |
| `C_hard` | 0.8115 | 0.89 | 4.47 | 16.05 | 4.42 | 19.42 % | 12.00 |
| `C_soft` | 0.8130 | 0.32 | 4.47 | 14.20 | 4.47 | 25.22 % | 12.00 |
| `D_range` | 0.8126 | **0.28** | 4.47 | **13.42** | 4.42 | 24.27 % | 12.00 |

*결과:* `σ_ray` median은 4.11 → 4.47 cm로 **약간 나빠졌고**(양자화 눈금 2.5 cm 위의
한 칸 차이), 전역 편향을 뺀 국소 잔차도 4.00 → 4.42 cm로 같은 방향이다. P90만 15.37 →
13.42 cm로 낫다.

**상태 불일치(어떤 시드는 장애물을 보고 어떤 시드는 격자 끝까지 free로 본 광선)는
18.5 % → 24.3 %로 5.7 pp 나빠졌다.** 그리고 **이 대가는 soft target에서 온다** —
`C_hard`는 19.42 %로 CE와 거의 같고, soft로 바꾸면 25.22 %가 된다. 집계 방식의 몫은 아니다.

*계획서 §4.2의 분기 판정:* `V_global`은 감소하고 `σ_local`은 감소하지 않는다.
→ **"전역 편향 성분만 안정화됐다."**

*주장하면 안 되는 것:* "예측 free-space geometry의 재현성이 개선되었다." 국소 성분은
그대로이거나 약간 나쁘다. 상태 불일치 악화는 설계 문서 §21.7이 옛 config에서 한계로 적시한
것과 같은 방향이고(21.9 % → 25.5 %), **여기서 그 원인이 soft target임이 밝혀졌다.**

---

## 5. free/non-free 결정의 재현성 (계획서 §5) — 개선되지 않았다

시드 **쌍**에 대한 평균 불일치율. 경계 근방은 `|d| ≤ 0.15 m`(valid의 12.7 %).

**선택 체크포인트**

| 칸 | D 전체 | D 경계근방 | D 대역밖 | 동작점 맞춘 뒤 D 전체 | **σ(τ_s)** |
|---|---|---|---|---|---|
| `A_ce` | 1.452 % | **6.588 %** | 0.717 % | 1.409 % | 0.0857 |
| `B_perset` | 2.883 % | 10.095 % | 1.850 % | 2.368 % | 0.0713 |
| `C_hard` | 1.511 % | 6.942 % | 0.733 % | 1.516 % | 0.0383 |
| `C_soft` | 1.443 % | 6.999 % | 0.647 % | 1.440 % | **0.0088** |
| `D_range` | **1.411 %** | 6.803 % | **0.639 %** | 1.422 % | **0.0105** |

**고정 epoch 40** — 결론이 같다.

| 칸 | D 전체 | D 경계근방 | 맞춘 뒤 D 전체 | σ(τ_s) |
|---|---|---|---|---|
| `A_ce` | 1.406 % | 6.260 % | 1.425 % | 0.0866 |
| `C_hard` | 1.423 % | 6.210 % | 1.490 % | 0.0233 |
| `C_soft` | 1.438 % | 7.015 % | 1.458 % | **0.0095** |
| `D_range` | 1.412 % | 6.828 % | 1.440 % | **0.0111** |

*결과:* **전체 map에서 1.45 % → 1.41 %로 사실상 동률**이고, 경계 근방에서는 6.59 % →
6.80 %로 약간 나쁘다. 동작점을 맞춰도 그대로다. **두 epoch 기준 모두에서 같다.**

**그런데 같은 표의 `σ(τ_s)`가 정반대를 말한다** — 같은 안전 수준을 만드는 문턱의 시드 간
산포가 0.086 → 0.011로 **8~9배** 작아지고, 고정 epoch에서도 그렇다(0.087 → 0.011).

*주장할 수 있는 것:* **"어느 셀이 free인가의 합의는 개선되지 않았다(경계 근방은 약간
악화). 그러나 확률 눈금 자체의 재현성은 크게 좋아졌다."** 둘은 같은 표에서 나왔으므로
한쪽만 인용하면 안 된다.

---

## 6. 동작점 안정성 (계획서 §6) — 두 번째로 큰 개선

### 6.1 목표 `free_miss`를 맞추는 문턱의 산포 `V_τ(q)`

| q(`free_miss`) | `A_ce` τ* | `C_hard` τ* | `C_soft` τ* | `D_range` τ* |
|---|---|---|---|---|
| 0.0681 | 0.358±**0.098** | 0.247±**0.038** | 0.409±**0.006** | 0.385±**0.009** |
| 0.0817 | 0.606±**0.087** | 0.463±**0.039** | 0.501±**0.009** | 0.478±**0.010** |
| 0.0953 | 0.790±**0.047** | 0.675±**0.031** | 0.579±**0.011** | 0.564±**0.012** |

*결과:* q=0.0817에서 **`A_ce` 0.087 → `C_hard` 0.039(2.2배) → `C_soft` 0.009(9.7배)**.
**두 변경이 모두 기여하고 soft target 쪽이 4.3배로 더 크다.** 유도량 `σ_τ_eff`도 같은
방향이다. §5의 고정 epoch 표에서도 유지된다.

*주장할 수 있는 것:* **"같은 안전 동작점을 만들기 위해 필요한 결정 문턱이 재학습에 훨씬 덜
흔들린다."** 배포 관점의 실질적 이득이다 — 다시 학습할 때마다 τ를 다시 캘리브레이션할
필요가 줄어든다. **이 축은 선택·고정 두 기준에서 모두 성립한다.**

### 6.2 같은 `free_miss`에서의 `fatal` — "안전 개선"은 동작점 이동이다

q=0.0817에서 `fatal`: `A_ce` **0.1208**±0.0040, `C_hard` 0.1240±0.0018,
`C_soft` 0.1214±0.0023, `D_range` 0.1230±0.0017.

*결과:* τ=0.5에서 `fatal`이 0.1284 → 0.1191(−0.93 pp)로 좋아 보이지만, **같은 `free_miss`
에서는 네 칸이 0.003 안에 모여 있고 CE가 가장 낮다.** `missed_obstacle`도 같다.

*주장할 수 있는 것:* "τ=0.5의 `fatal` 감소는 representation 개선이 아니라 **동작점
이동**이다. CE도 τ를 올리면 같은 자리에 온다."
*주장하면 안 되는 것:* safety–conservativeness 교환 곡선이 개선되었다는 것.
**설계 문서 §16.2가 옛 config에서 내린 결론이 새 config·새 대조군에서 재확인됐다.**

---

## 7. 짝지은 비교 (계획서 §10)

`D_range − A_ce`, 시드별 (선택 epoch).

| 지표 | s0 | s1 | s2 | s3 | s4 | 평균 | 방향 일관성 |
|---|---|---|---|---|---|---|---|
| `iou_free` | +0.0012 | +0.0009 | +0.0006 | +0.0020 | +0.0009 | +0.0011 | 5/5 (크기는 σ_run 규모) |
| `fatal` | −0.0138 | −0.0102 | −0.0065 | −0.0104 | −0.0054 | −0.0093 | **5/5 감소** |
| `free_miss` | +0.0141 | +0.0124 | +0.0061 | +0.0099 | +0.0042 | +0.0093 | **5/5 증가** |
| `f1@10cm` | −0.0097 | −0.0044 | −0.0088 | −0.0139 | **+0.0039** | −0.0066 | 4/5 감소 |

*결과:* 방향이 5/5인 둘(`fatal`↓ / `free_miss`↑)과 4/5인 `f1@10cm`↓은 **정확히 동작점 이동이
만드는 무늬**다 — 더 보수적으로 예측하면 셋이 그렇게 움직인다. §6.2가 그 해석을 확정한다.

> cuDNN 비결정성 때문에 같은 시드도 완전히 같은 조건이 아니다(σ_run = 0.0009, §1.3).
> 이 표는 p-값이 아니라 **방향 일관성**을 보는 용도다.

---

## 8. 시퀀스 단위 (계획서 §7) — 이 split으로는 답할 수 없다

val 시퀀스가 `rawos3`(37프레임)와 `raws1`(38프레임) 둘뿐이라 "시퀀스 간 SD"는 두 값의
차이일 뿐이다. 분해 자체는 `report_per_sequence.txt` / `per_sequence.json`에 남겼다.

*정본은 따로 있다:* LOSO 7-fold × 시드 3(진단 문서 §31)에서 `σ_fold = 0.0405`로
**`σ_seed`의 27배**였다. 시퀀스 변동이 시드 변동을 압도한다. **검토서 P4(독립 test set)는
수행하지 않았다** — 별도 test 시퀀스가 없고, 만들려면 train 192프레임을 더 줄여야 하며,
이 질문의 정본이 LOSO이기 때문이다(사용자 판단).

---

## 9. 계획서 §12의 여덟 질문에 대한 답

| # | 질문 | 답 |
|---|---|---|
| 1 | 되올림을 얼마나 줄였나 | **45.0 % → 0.6 %.** 원인은 **집계 방식이 아니라 대역 target의 hard/soft**다(`C_hard` 47.6 %) |
| 2 | 전역 편향이 덜 바뀌나 | **그렇다.** σ_전역 1.27 → 0.28 cm(4.5배). 집계 1.4배 + soft target 2.8배 |
| 3 | 편향 제거 후 국소 기하가 더 일관적인가 | **아니다.** σ_잔차 4.00 → 4.42 cm |
| 4 | 같은 셀의 상태가 덜 뒤집히나 | **아니다.** 1.45 → 1.41 %(동률), 경계 근방 6.59 → 6.80 % |
| 5 | 평균 품질을 희생하지 않고 재현성이 개선됐나 | **품질은 희생하지 않았다.** 최종 지표 재현성은 **개선 근거 없음**(고정 epoch에서 유의 없음) |
| 6 | τ=0.5의 차이가 보정 이동인가 | **보정 이동이다.** 같은 `free_miss`에서 CE가 가장 낮다 |
| 7 | 시퀀스 전반에서 개선됐나 | **답할 수 없다**(val 시퀀스 2개) |
| 8 | 어디까지 근거 있게 말할 수 있나 | **objective convergence stability: 예**(§2.1). **경계 국소성: 예**(§2.2). **objective–quality alignment: 예**(§2.3). **operating-point reproducibility: 예**(§6.1). **retraining reproducibility: 아니오**(§3.2). **prediction stability: 아니오**(§4·§5). **generalization stability: 미확인**(§8) |

### 한 문장

> **경계를 확정적인 0/1로 감독한 것이 목적함수를 망가뜨리고 있었다. 그 불확실성을 loss에
> 모델링하면 목적함수가 정상화되고 확률 눈금이 재현 가능해진다. 예측 자체는 거의 그대로다.**

### 쓸 수 있는 표현 / 쓰면 안 되는 표현

- ✅ "hard boundary certainty가 validation objective의 misalignment와 rebound의 원인이고,
  soft target이 그것을 해결한다" (§2.1 — `C_hard` 대조군이 근거)
- ✅ "CE의 validation 오차 증가는 경계 대역에 국소화돼 있다(증가분의 96.5 %)" (§2.2)
- ✅ "목적함수가 checkpoint selection에 쓸 수 있는 신호로 회복되었다" (§2.3)
- ✅ "같은 안전 동작점을 만드는 결정 문턱이 재학습에 훨씬 덜 흔들린다" (§6.1)
- ✅ "새 loss는 기존의 predictive behavior를 크게 바꾸지 않으면서 목적함수의 거동과 품질
  proxy와의 정렬을 정상화했다" (§3.1)
- ❌ "전체 학습 안정성이 개선되었다" — 축을 나눠 말한다
- ❌ "retraining reproducibility가 개선되었다" — §3.2에서 성립하지 않는다
- ❌ "prediction stability가 개선되었다" — §4·§5에서 반대 방향
- ❌ "안전성이 개선되었다" — §6.2 동작점 이동
- ❌ "정확도가 개선되었다" — §3.1 판정 불가

---

## 10. `L_range`(λ_R=0.3)에 대한 판단

| | `C_soft` | `D_range` | |
|---|---|---|---|
| `iou_free` | 0.8130±0.0008 | 0.8126±0.0005 | 노이즈 |
| `f1@10cm` | 0.6010±0.0040 | 0.6032±0.0057 | 노이즈 |
| `fatal` | 0.1215±0.0007 | 0.1191±0.0007 | 소폭 개선(동작점 이동 방향) |
| σ_전역 | 0.32 cm | 0.28 cm | 소폭 개선 |
| σ(τ_s) @ep40 | 0.0095 | 0.0111 | 소폭 악화 |
| Δep(loss↔iou) | 2.2 | 9.8 | 악화 |
| `iou_free` σ @ep40 | 0.0008 | 0.0005 | 소폭 개선 |

*결과:* **일관되고 명확한 추가 이득이 없다.** 지표마다 방향이 갈리고 크기가 σ 규모다.

> **2026-09-01 판본의 "`L_range`가 손해다"를 철회한다.** 그 결론은 선택 epoch 기준의
> 재현성 배율에 기댄 것이었고, 고정 epoch에서는 격차가 대부분 사라지거나 역전된다
> (`free_miss` σ 2.9배 악화 → 0.8배, `range_bias` 1.7배 → 0.7배). 검토서 §3의 지적이 옳았다.

*방어 가능한 표현:* **"ray-wise auxiliary term에서 평균 예측 품질이나 재현성의 일관된 추가
이득은 관찰되지 않았다."** 확정 config를 바꾸자는 제안은 하지 않는다 — 프로젝트 방침이
"결과가 예상과 달라도 새 가지를 열지 않는다"이고, `λ_R` 재스윕은 새 가지다. 다만 **논문에서
`L_range`를 기여로 주장하려면 이 표를 넘어야 한다.**

---

## 11. 원시 데이터

전부 `runs/loss_effect/analysis/`. **결과를 대표하는 파일은 `RESULTS.json` 하나**이고,
폴더 안내는 같은 곳 `README.md`에 자동 생성된다.

| 파일 | 내용 |
|---|---|
| **`RESULTS.json`** (117 KB) | ★ 핵심 결과 전부. 환경·config·무결성·일곱 축·칸별 집계·런별 행. 숫자마다 출처 표기 |
| **`RESULTS.csv`** (1,050행) | ★ 같은 내용의 긴 형식 표(`scope` = run / cell_mean / cell_sd / axis) |
| `scalars.csv` | **1차 원본.** 25런 × 전 지표 × 전 epoch. 태그를 고르지 않았다 |
| `configs.json` · `manifest.json` | 런마다 config 전체 / 완주 여부·태그 목록 |
| `predictions/` (50 + 1개) | **1차 원본.** val 확률맵(τ 적용 전) — 선택 체크포인트와 고정 epoch 둘 다 + GT 라벨 + 부호 거리장 |
| `threshold_sweep_rows*.csv` | **1차 원본.** 셀 × 시드 × τ(11점)의 모든 지표 |
| `verify_predictions.json` | 무결성 검사 결과 |
| `seed_jitter/` · `*.json` · `report_*.txt` | 광선별 원시 배열 / 축별 요약 / 도구 출력 전문 |

**옛 실행 둘은 `runs/archive/`에 로그만 남겼다**(각 9 MB). 체크포인트와 확률맵은 지웠다 --
전자는 이 진단 로깅이 없는 코드에서, 후자는 잘못된 환경에서 나온 것이라 대체됐다.
남긴 `logs/`와 `analysis/scalars.csv`가 §1.3의 σ_run과 §12.1의 환경 비교 근거다.

### 무결성 — 400건 전부 통과

`tools/verify_val_predictions.py`가 저장된 확률맵으로 지표를 **처음부터 다시 계산해** 학습
루프가 그 epoch에 기록한 값과 대조한다. 전혀 다른 코드 경로로 잰 두 값이 맞으면 프레임
순서·기하·체크포인트 선택이 전부 맞은 것이다. **50개 파일 × 8지표 = 400건 전부 통과,
최대 차이 3.6e-04**(float16 저장의 반올림 수준).

### 재실행 재현성 — 파이프라인 전체를 두 번 돌려 대조했다

`runs/archive/loss_effect_v1/`은 영역별 CE 진단을 넣기 **전에** 같은 config·같은 시드로 돌린
20런이다. 18쌍을 대조한 결과 `iou_free` @ep40의 차이가 평균 +0.0002, 최대 0.0017,
표준편차 **0.0009**이고 부호가 섞인다. **① 추가한 진단은 학습에 영향이 없고
② 재실행 노이즈 σ_run = 0.0009**라는 두 결과를 동시에 준다(§1.3).

---

## 12. 환경 — 정본에서 돌렸다

| | 값 |
|---|---|
| conda | **`bev-chamdog`** |
| Python | 3.11.15 |
| torch | **2.7.0+cu128** (GPU가 sm_120이라 빌드가 중요하다 — `AGENTS.md` 개발 환경) |
| numpy | 1.26.4, 임시 소거 결함 **없음** |

### 12.1 환경 차이가 결과를 움직였나 — **재실행 노이즈와 구별되지 않는다**

`base`(Python 3.14 + torch 2.13)에서 돌린 20런(`runs/archive/loss_effect_wrongenv`)과
정본 환경의 같은 20런(`runs/archive/loss_effect_v1`)을 **같은 config·같은 시드**로 대조했다.
비교 기준은 같은 환경에서 따로 실행한 짝의 차이(σ_run)다.

| 지표 (ep40) | 환경 차이 \|Δ\| 평균 | 최대 | σ_run(같은 환경) | 배율 |
|---|---|---|---|---|
| `iou_free` | 0.0007 | 0.0022 | 0.0008 | **1.0배** |
| `fatal_rate` | 0.0008 | 0.0016 | 0.0007 | 1.1배 |
| `free_miss_rate` | 0.0009 | 0.0023 | 0.0011 | 0.8배 |
| `f1@10cm` | 0.0038 | 0.0087 | 0.0030 | 1.3배 |

**배율이 전부 1 근처다** — torch 2.13/cu130에서 학습한 것과 2.7.0/cu128에서 학습한 것의
차이가 **같은 환경에서 다시 돌린 차이와 구별되지 않는다.** 즉 환경 사고는 숫자를 틀리게
만들지 않았다. 재실행의 근거는 **출처의 일관성**이다 — 프로젝트의 다른 모든 런이 정본
환경에서 나왔고, 논문에서 그 숫자들과 나란히 실리려면 같은 환경이어야 한다.

> **2026-09-01에 20런 전체를 `base`(Python 3.14 + torch 2.13)에서 돌린 사고가 있었다.**
> 그 환경의 numpy는 **256 KB를 넘는 배열에 `~`를 쓰면 원본을 제자리에서 뒤집는다**
> (numpy 1.26은 Python 3.12까지만 지원). 예외도 경고도 없어서 분석 도구 하나가 조용히 틀린
> 표를 냈다. **정본 환경에는 이 결함이 없다**(진단 `tools/check_numpy_elision.py`가 전 크기
> `OK`). 재발 방지로 학습·분석 스크립트에 **환경 가드**를 넣었다 — `bev-chamdog`가 아니면
> 시작되지 않는다(예외는 `ALLOW_ANY_ENV=1`). 방어 코드 `projects/common/npsafe.bool_not`은
> 맞는 환경에서도 안전한 관용구이므로 남겨 뒀다.

---

## 13. 검토 코멘트 처리 내역

[`docs/temp/loss_effect_review_for_claude_2026-09-02.md`](temp/loss_effect_review_for_claude_2026-09-02.md)에 대한 항목별 처리.

| 항목 | 처리 |
|---|---|
| §1.2 "val 라벨 없이" 표현이 부정확 | **채택.** §2.3에서 정정 |
| §2.3 per-set averaging 단정 금지 | **채택하고 실험으로 해결.** `C_hard`를 추가해 분해했고, **결론이 반대로 바뀌었다**(원인은 target) |
| §3 `L_range` 선택 편향 유보 | **채택.** 고정 epoch에서 검증해 "손해다"를 철회(§10) |
| §4 안정성 용어 분리 | 이미 반영돼 있었고 §9에 표현 목록으로 정리 |
| §5 성능을 결과 중심에서 뺄 것 | **절반 채택.** 향상을 주장하지 않되 **"차이 없음"은 보고한다**(§1.5) |
| §6.1 지원 환경에서 재실행 | **채택하고 더 나아갔다.** 분석만이 아니라 **학습까지** 정본 환경에서 다시 돌렸다 — 검토서가 몰랐던 사실(학습이 torch 2.13에서 돌았고 정본은 2.7)이 있었다 |
| §6.1 provenance 불일치 지적 | **정확한 지적.** 그 환경 사고의 흔적이었고 재실행으로 해소 |
| P2 `C_hard` 대조군 | **채택.** 코드 변경 없이 `SIGMA_M=0.001`로 구현. **이 실험의 핵심 결과가 여기서 나왔다** |
| P3 boundary/confident 분해 | **채택.** loss와 무관한 공통 눈금을 학습 로그에 추가(§2.2) |
| P4 독립 test set | **미실시.** test 시퀀스가 없고 정본이 LOSO다(사용자 판단) |
| P5 고정 epoch 요약 | **채택.** §1.4·§3.2·§5에 반영. **재현성 주장 셋이 이 검사로 철회됐다** |
| P6 시드 확대 | **미실시.** 재현성 주장 자체를 접었으므로 시드를 늘려 살릴 대상이 없다(사용자 판단) |

**검토서가 자기 지적을 끝까지 밀지 않은 곳이 하나 있다.** §6.6은 "soft boundary
supervision improves retraining reproducibility"를 headline 후보로 보고 시드 확대를
권했는데, §3의 선택 편향 논리를 그 주장에 적용하면 **고정 epoch에서 무너진다**(§3.2).
그래서 그 주장은 철회했고 시드 확대도 하지 않았다.

---

## 14. 재현 방법

```bash
conda activate bev-chamdog          # 정본 환경. 아니면 스크립트가 멈춘다
python tools/check_numpy_elision.py # 환경 점검 (전부 OK여야 한다)

# 학습 25런: GPU 두 개로 나누면 약 90분
CUDA_VISIBLE_DEVICES=1 SEEDS="4 3"   bash configs/loss_effect.sh &
CUDA_VISIBLE_DEVICES=0 SEEDS="0 1 2" bash configs/loss_effect.sh

# 원시 보존 -> 무결성 -> 집계 -> 결과 묶음 (약 12분)
CUDA_VISIBLE_DEVICES=0 bash configs/loss_effect_analysis.sh
```
